# basic-kafka

Minimal spring-kafka project - if you just want to produce and consume a message

run **docker-compose up** to start kafka server

For starting the app run **gradlew bootRun**

For more detailed (and complex) spring-kafka producer and consumer check https://github.com/sowieso-fruehling/kafka-playground
